import { Injectable } from '@angular/core';


export const AUTHENTICATE_USER_ID = 'AuthenticateUserId';

@Injectable()
export class StorageService {
    // constructor(
    //     private cookieService: CookieService
    // ) {}

    // setAuthCookie(data: string): void {
    //     this.cookieService.set(AUTHENTICATE_USER_ID, data, { path: '/'});
    // }

    // getAuthCookie(): string {
    //     return this.cookieService.get(AUTHENTICATE_USER_ID);
    // }
}